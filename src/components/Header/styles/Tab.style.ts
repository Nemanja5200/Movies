import styled from 'styled-components';

export const TabStyle = styled.nav`
    display: flex;
    justify-content: center;
    align-items: center;
    height: 40px;
    gap: 40px;
    color: gold;

    a {
        &:hover {
            opacity: 0.8;
        }
    }
`;
